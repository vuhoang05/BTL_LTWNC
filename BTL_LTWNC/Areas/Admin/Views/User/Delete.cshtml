@using BTL_LTWNC.Models.EF
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model BTL_LTWNC.Models.EF.User

@if (Model == null)
{
    <h1 class="text-center text-uppercase text-danger fw-bold">Không có bản ghi nào!!!</h1>
}
else
{
    <div class="p-5">
        <h1 class="text-center text-uppercase font-bold">Xóa loại sản phẩm</h1>

        <div>
            <h4>Thông tin loại sản phẩm</h4>

            <dl class="row pt-3">
                <dt class="col-sm-2">
                    ID:
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.iUserID)
                </dd>
                <dt class="col-sm-2">
                    Role:
                </dt>
                <dd class="col-sm-10">
                    @* @Html.DisplayFor(model => model.iUserRoleID) *@
                    @ViewBag.roleName
                </dd>
                <dt class="col-sm-2">
                    username:
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.sUserName)
                </dd>
            </dl>

            <form method="post" asp-area="Admin" asp-controller="User" asp-action="Delete">
                <input type="hidden" asp-for="iUserID" />

                <div class="d-flex gap-4">
                    <a href="/admin/user/QuanLyNguoiDung" class="btn btn-primary">Quay lại</a>
                    <button type="submit" class="btn btn-danger">Xóa</button>
                </div>
            </form>
        </div>
    </div>
}
